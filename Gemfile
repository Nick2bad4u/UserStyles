source "https://rubygems.org"

# Pages installs this bundle in deployment mode using the committed lockfile.
# Keep the GitHub Pages plugin stack pinned when refreshing transitive gems.
gem "github-pages", "= 232", group: :jekyll_plugins
